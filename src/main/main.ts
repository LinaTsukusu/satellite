import {Satellite} from '@/main/satellite'
import {SatellitePlugin} from '@/main/satellite-plugin'


function loadPlugin(plugin: SatellitePlugin) {
  Object.keys(SatellitePlugin)
}

function main() {
  const satellite = Satellite.instance

}
